import 'package:flutter/material.dart';

class VisitedHistory extends StatelessWidget {
  const VisitedHistory({super.key});
  static String route = "VisitedHistory";
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("VisitedHistory"));
  }
}
